{
  "name": "codex-cursor-proxy-local",
  "version": "0.2.0-local",
  "private": true,
  "type": "module",
  "description": "Local Cursor-compatible proxy for ChatGPT Codex sessions.",
  "bin": {
    "codex-cursor-proxy": "dist/index.js"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js"
  },
  "engines": {
    "node": ">=18.17"
  },
  "devDependencies": {
    "@types/node": "^20.17.57",
    "esbuild": "^0.25.10",
    "javascript-obfuscator": "^4.1.1",
    "typescript": "^5.8.3"
  }
}
